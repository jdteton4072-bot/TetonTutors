# Prompt Pack — {{TITLE}}

Derived from [`brief.md`](./brief.md) v0.1 ({{DATE}}).

Each card is self-contained: copy the fenced block into the named tool and nothing else is
needed. `Runs on` says who does the work — **Claude Code** means an agent can execute it in
this repo; **external generator** means a person pastes it into an image, video, or audio
tool.

**Run order:** style frame → cast & asset sheets → hero deliverable → derivatives.
Audio and copy can run in parallel with the hero deliverable.

## Cast & assets

<!-- Canonical description blocks for anything reused across prompts (characters, props,
     locations). Paste these verbatim into each card that uses them — paraphrasing between
     prompts is what makes a character's face change between shots. Edit here only. -->

## Prompts

### P-01 — <name>

**Deliverable:**
**Tool:**
**Runs on:**

```
<prompt text, verbatim and self-contained>
```

**Negative / avoid:**
**Parameters:**
**Acceptance:**
**Derives from:**

## Not covered

<!-- Deliverables from the brief with no prompt card, and why — live action, licensed
     footage, a human designer's job. Listing these is what makes the pack's coverage
     checkable. -->
